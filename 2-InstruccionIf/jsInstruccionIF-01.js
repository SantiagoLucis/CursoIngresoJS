function mostrar()
{
	//tomo la edad 
	var edad;
	var condicion;
	edad=txtIdEdad.value;

	/*verdadero = 1 = true
	falso = 0 = false todo lo que no sea falso es verdadero(todo lo que no sea 0 es verdadero)*/

	//relaciones: <(menor que), >(mayor que), <=(menor o igual que), >=(mayor o igual que), !=(distinto de), ==(igual a);

	//condicion = 9>7;
	//alert (condicion);

	if (edad==15) //despues del "if" no se pone ; porque lo anula
	{
		alert("Niña bonita");
	}
}//FIN DE LA FUNCIÓN