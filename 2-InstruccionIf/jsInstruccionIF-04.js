function mostrar()
//FIN DE LA FUNCIÓN
// con >=, <= (octavio dijo que hagamos sin >=, <=)
/*{
	//tomo la edad  
	var edad;
	edad=txtIdEdad.value;

	    if (edad >= 13) 
	{ 
		if(edad <= 17)
	{ 
		alert("sos adolescente");
	}}
}*/

{
	var edad;
	edad= txtIdEdad.value;
	edad=parseInt(edad);

	if(edad > 12)
{

		if(edad < 18)
{
			alert("sos adolescente");
}}}
/*{
	var edad;
	edad= txtIdEdad.value;
	edad=parseInt(edad);

	if(edad > 12 && edad <18)
{
		alert("sos adolescente");
}}*/

