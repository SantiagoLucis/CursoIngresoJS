function mostrar()
{
	//tomo la edad  
	var edad;
	edad=txtIdEdad.value;

	if (edad >= 13) { if(edad <= 17){ 
		alert("sos adolescente");
	}}

	alert("tenes " +edad+ " años");

//no resuelto.
}//FIN DE LA FUNCIÓN